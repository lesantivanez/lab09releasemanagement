events {}

http {
    upstream app {
        server blue:3000;
        # server green:3000;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://app;
        }
    }
}